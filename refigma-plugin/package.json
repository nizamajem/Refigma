{
  "name": "refigma-plugin",
  "version": "0.1.0",
  "description": "Figma plugin companion for Refigma AI",
  "license": "UNLICENSED",
  "scripts": {
    "dev": "vite build --mode development --watch --clearScreen=false",
    "dev:once": "vite build --mode development",
    "build": "vite build --mode production --clearScreen=false",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "axios": "^1.12.2"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.118.0",
    "concurrently": "^9.2.1",
    "esbuild": "^0.25.11",
    "sass": "^1.93.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3",
    "vite": "^6.4.0"
  }
}
